<h1>Events#index</h1>

<% @events.in_groups_of(3, false).each do |group| %>
  <div class="row">
    <% group.each do |event| %>
      <div class="col s4">
        <%= render 'event', event: event %>
      </div>
      <% end %>
  </div>
<% end %>
